package domini;

interface FileCodec {
    public static void compress(IO.Char.reader input, IO.Char.writer output) { }
    public static void decompress(IO.Char.reader input, IO.Char.writer output) { }
}
